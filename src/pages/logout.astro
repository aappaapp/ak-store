---
Astro.cookies.delete("token", { path: "/" });
return Astro.redirect(
  `${import.meta.env.COGNITO_URL}/logout?client_id=${import.meta.env.APP_CLIENT_ID}&logout_uri=${Astro.url.origin}`,
  302,
);
---
