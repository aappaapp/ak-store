---
Astro.cookies.delete("token", { path: "/" });
return Astro.redirect(
  `https://ak-store.auth.us-east-1.amazoncognito.com/logout?client_id=${import.meta.env.APP_CLIENT_ID}&logout_uri=http://localhost:4321/`,
  302,
);
---
